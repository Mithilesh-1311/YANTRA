{
  "name": "yantra-fullstack",
  "version": "1.0.0",
  "description": "Yantra Hackathon Fullstack App",
  "scripts": {
    "start": "node Backend/server.js",
    "build": "npm install && cd Frontend && npm install && npm run build && cd ../Backend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@supabase/supabase-js": "^2.38.4"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}